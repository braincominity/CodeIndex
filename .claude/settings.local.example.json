{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Optional. Copy to .claude/settings.local.json only if you accept that gh may need local auth material to create issues/PRs. Do not commit settings.local.json.",
  "sandbox": {
    "filesystem": {
      "allowRead": [
        "~/.config/gh"
      ]
    },
    "network": {
      "allowedDomains": [
        "github.com",
        "api.github.com",
        "uploads.github.com"
      ]
    }
  }
}
