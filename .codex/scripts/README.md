# Agent Scripts

This directory may contain helper scripts for repeated agent workflows.
Scripts are optional; workflows remain authoritative.

Do not encode hidden policy in scripts only. If a rule matters, document it in `AGENT_GUIDE.md` or `.codex/workflows/`.
