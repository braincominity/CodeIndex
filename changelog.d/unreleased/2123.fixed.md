---
category: fixed
issues:
  - 2123
affected:
  - src/CodeIndex/Indexer/References/Languages/DockerfileReferenceExtractor.cs
  - tests/CodeIndex.Tests/ReferenceExtractorTests.cs
---

## English

- **Dockerfile reference extraction now indexes nested parameter expansion variables (#2123)** — variables inside nested `${...}` defaults and alternates, such as `${PRIMARY:-${FALLBACK}}`, are now emitted as references across the supported conditional expansion operators.

## 日本語

- **Dockerfile の参照抽出がネストしたパラメータ展開内の変数を index するようになりました (#2123)** — `${PRIMARY:-${FALLBACK}}` のようなネストした `${...}` のデフォルト値や代替値に含まれる変数を、対応済みの条件付き展開演算子全体で参照として出力するようになりました。
