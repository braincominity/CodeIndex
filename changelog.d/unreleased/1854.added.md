---
category: added
issues:
  - 1854
affected:
  - src/CodeIndex/Cli/HookCommandRunner.cs
  - src/CodeIndex/Cli/IndexCommandRunner.cs
  - src/CodeIndex/Cli/JsonOutputContracts.cs
  - USER_GUIDE.md
  - samples/git-hooks/pre-commit
---

## English

- **Added first-class git pre-commit hook integration (#1854)** — `cdidx hooks install|status|uninstall` can manage a repository pre-commit hook that runs `cdidx index . --quiet`, chains an existing hook, and keeps indexing errors visible.

## 日本語

- **git pre-commit hook 連携を正式サポートしました (#1854)** — `cdidx hooks install|status|uninstall` で `cdidx index . --quiet` を実行する pre-commit hook を管理でき、既存 hook の chain と indexing error の表示も維持します。
