---
category: fixed
issues:
  - 1865
affected:
  - src/CodeIndex/Database/DbSymbolReader.cs
  - src/CodeIndex/Cli/QueryCommandRunner.cs
  - src/CodeIndex/Models/QueryResults.cs
---

## English

- **Outline entries now disambiguate overloads and shadowed symbols (#1865)** — `outline` JSON/MCP output includes compact `display_name` and symbol-level `path` fields, and human output uses compact callable signatures such as `Process(string)`.

## 日本語

- **`outline` が overload や shadowed symbol を判別できるようになりました (#1865)** — `outline` の JSON/MCP 出力に compact な `display_name` と symbol-level の `path` を追加し、人間向け出力でも `Process(string)` のような callable signature を表示します。
