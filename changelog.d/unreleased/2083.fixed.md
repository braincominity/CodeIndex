---
category: fixed
issues:
  - 2083
affected:
  - src/CodeIndex/Database/DbWriter.cs
  - src/CodeIndex/Cli/IndexCommandRunner.cs
  - src/CodeIndex/Mcp/McpToolHandlers.cs
  - tests/CodeIndex.Tests/DatabaseTests.cs
---

## English

- **TypeScript merged declarations now emit augmentation references (#2083)** — duplicate TypeScript `interface` and `type` declarations are connected with `augmentation` references after indexing, so merged surfaces are visible to reference and impact queries.

## 日本語

- **TypeScript の merged declaration が augmentation 参照を出すようになりました (#2083)** — TypeScript の同名 `interface` / `type` 宣言を index 後に `augmentation` 参照で接続し、merged surface が references / impact query から見えるようになりました。
