---
category: fixed
issues:
  - 2253
affected:
  - src/CodeIndex/Cli/IndexCommandRunner.cs
  - tests/CodeIndex.Tests/IndexCommandRunnerTests.cs
  - tests/CodeIndex.Tests/QueryCommandRunnerTests.cs
---

## English

- **JSON full scans now emit liveness on stderr (#2253)** - `cdidx index <projectPath> --json` now reports scan/index/optimize progress to stderr while preserving stdout for the final JSON payload.

## 日本語

- **JSON full scan が stderr に liveness を出すようになりました (#2253)** - `cdidx index <projectPath> --json` は、stdout を最終 JSON payload 専用に保ったまま、scan / index / optimize の進捗を stderr に出力します。
