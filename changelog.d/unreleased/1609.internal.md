---
category: internal
issues:
  - 1609
affected:
  - .editorconfig
  - Directory.Build.props
  - .github/workflows/dotnet.yml
  - DEVELOPER_GUIDE.md
---

## English

- **Formatting and warning drift are now gated (#1609)** — the repository has a root `.editorconfig`, CI verifies `dotnet format`, and builds treat compiler warnings as errors with an explicit allowlist for existing trim-analysis warnings.

## 日本語

- **format と warning の drift を CI で検出するようになりました (#1609)** — repository root に `.editorconfig` を追加し、CI が `dotnet format` を検証し、build では既存の trim-analysis warning を明示 allowlist に入れたうえで compiler warning を error として扱います。
