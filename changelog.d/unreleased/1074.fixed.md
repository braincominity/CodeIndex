---
category: fixed
issues:
  - 1074
affected:
  - src/CodeIndex/Cli/QueryCommandRunner.cs
  - src/CodeIndex/Cli/JsonOutputContracts.cs
  - tests/CodeIndex.Tests/QueryCommandRunnerTests.cs
---

## English

- **QueryCommandRunner's remaining anonymous JSON outputs are trim-safe now (#1074 follow-up)** — The JSON paths for `search`, `definition`, `files`, `find`, `validate`, and `inspect` no longer use anonymous types; they now use source-generated DTOs or `JsonObject` builders. Added a trimmed-publish regression that verifies search counts, `find` counts, empty `validate` output, and the `inspect` missing-file error JSON. This directly addresses the follow-up candidates from PR #1074.

## 日本語

- **QueryCommandRunner の残りの匿名 JSON 出力も trim-safe 化しました (#1074 の follow-up)** — `search`、`definition`、`files`、`find`、`validate`、`inspect` の JSON 経路から匿名型をなくし、source-generated DTO か `JsonObject` ビルダーに置き換えました。trimmed publish した `cdidx.dll` に対する regression も追加し、検索カウント、`find` カウント、`validate` の空結果、`inspect` の missing-file エラー JSON を検証します。これは PR #1074 の Follow-up candidates に直接対応する修正です。
