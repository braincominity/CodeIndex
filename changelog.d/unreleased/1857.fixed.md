---
category: fixed
issues:
  - 1857
affected:
  - src/CodeIndex/Cli/QueryCommandRunner.cs
  - src/CodeIndex/Cli/CliFlagSchema.cs
  - src/CodeIndex/Cli/ConsoleUi.cs
  - tests/CodeIndex.Tests/QueryCommandRunnerTests.cs
  - README.md
  - USER_GUIDE.md
---

## English

- **`status` now explains readiness fields in human output (#1857)** — human `status` output translates raw readiness flags into user-facing labels with degraded reasons/remediation, and `status --explain <field>` prints a focused description without changing the existing JSON contract.

## 日本語

- **`status` が人間向け出力で readiness field を説明するようになりました (#1857)** — 人間向け `status` 出力は raw readiness flag をユーザー向けラベル、degraded reason、対処に翻訳し、既存 JSON contract は変えずに `status --explain <field>` で個別説明を表示できるようになりました。
