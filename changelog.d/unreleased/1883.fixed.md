---
category: fixed
issues:
  - 1883
affected:
  - src/CodeIndex/Database/DbReader.GraphQueries.cs
  - src/CodeIndex/Models/QueryResults.cs
  - src/CodeIndex/Mcp/McpToolHandlers.cs
  - tests/CodeIndex.Tests/DbReaderTests.cs
  - DEVELOPER_GUIDE.md
---

## English

- **Impact traversal now reports cycle termination metadata (#1883)** — `impact` and MCP `impact_analysis` now expose `termination_reason`, `cycle_detected`, and `cycles` so caller cycles are distinguishable from natural completion, max-depth termination, and row-limit truncation.

## 日本語

- **impact traversal が cycle termination metadata を報告するようになりました (#1883)** — `impact` と MCP `impact_analysis` は `termination_reason`、`cycle_detected`、`cycles` を公開し、caller cycle を自然終了、max-depth 到達、row-limit truncation と区別できるようになりました。
