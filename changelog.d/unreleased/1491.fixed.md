---
category: fixed
issues:
  - 1491
---

English:
- Added regression coverage for `cdidx index --files` so extension-change renames remove the stale old path from the index.

日本語:
- `cdidx index --files` で拡張子変更を伴うリネーム後に古いパスがインデックスから削除されることを回帰テストで固定しました。
