---
category: fixed
issues:
  - 1103
affected:
  - src/CodeIndex/Cli/JsonOutputContracts.cs
  - src/CodeIndex/Cli/QueryCommandRunner.cs
  - tests/CodeIndex.Tests/QueryCommandRunnerTests.cs
---

## English

- **The remaining `QueryCommandRunner` JSON outputs are trim-safe now (#1103 follow-up)** — the last anonymous JSON payloads in `search`, `files`, `find`, `symbols`, `outline`, and `validate` now use source-generated DTOs or `JsonObject` builders. Added a trimmed-publish regression that exercises the JSON count paths and the missing-file outline error.

## 日本語

- **`QueryCommandRunner` の残りの JSON 出力も trim-safe になりました (#1103 の follow-up)** — `search`、`files`、`find`、`symbols`、`outline`、`validate` に残っていた匿名 JSON payload を source-generated DTO か `JsonObject` builder に置き換えました。trimmed publish 版で JSON の count 系パスと、`outline` の missing-file エラーも確認する回帰テストを追加しています。
